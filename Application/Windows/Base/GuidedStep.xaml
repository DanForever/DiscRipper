<windows:ThemedWindow x:Class="DiscRipper.Windows.Base.GuidedStep"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:windows="clr-namespace:DiscRipper.Windows.Base"
        mc:Ignorable="d"
        Title="{Binding Title, RelativeSource={RelativeSource Self}}"
        Height="550" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <Control DockPanel.Dock="Left" Width="48" Height="32" Margin="5" Template="{StaticResource DiscIconTemplate}" />
            <ui:TitleBar Title="Guided submission" FontWeight="SemiBold" />
        </DockPanel>
        
        <DockPanel Grid.Row="1">
            <TextBlock DockPanel.Dock="Top" TextWrapping="WrapWithOverflow" Margin="30,5,30,5" FontSize="32" FontWeight="DemiBold" Text="{Binding Title, RelativeSource={RelativeSource AncestorType=windows:GuidedStep}}" />
            <TextBlock DockPanel.Dock="Top" TextWrapping="WrapWithOverflow" Margin="30,5,30,5" Text="{Binding Description, RelativeSource={RelativeSource AncestorType=windows:GuidedStep}}" />

            <TextBlock DockPanel.Dock="Top" />
            <ContentPresenter DockPanel.Dock="Top" Margin="30,5,30,5" Content="{Binding InnerContent, RelativeSource={RelativeSource AncestorType={x:Type windows:GuidedStep}}}" />

            <Grid DockPanel.Dock="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ui:Button Grid.Column="0" Margin="10" VerticalAlignment="Stretch" Click="Cancel_Click">
                    <Viewbox Stretch="Uniform">
                        <TextBlock Text="Cancel" FontSize="32" Margin="20,10,20,10" />
                    </Viewbox>
                </ui:Button>
                <ui:Button Grid.Column="1" Margin="10" VerticalAlignment="Stretch" Click="AdvancedMode_Click">
                    <Viewbox Stretch="Uniform">
                        <TextBlock Text="Advanced" FontSize="32" Margin="20,10,20,10" />
                    </Viewbox>
                </ui:Button>
                <ui:Button Grid.Column="2" Margin="10" VerticalAlignment="Stretch" Click="Previous_Click">
                    <Viewbox Stretch="Uniform">
                        <TextBlock Text="Previous" FontSize="32" Margin="20,10,20,10" />
                    </Viewbox>
                </ui:Button>
                <ui:Button Grid.Column="3" Appearance="Primary" Margin="10" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Click="Next_Click">
                    <Viewbox Stretch="Uniform">
                        <TextBlock Text="Next" FontSize="48" Margin="20,10,20,10" />
                    </Viewbox>
                </ui:Button>
            </Grid>
            
            <TextBlock />
            
        </DockPanel>
    </Grid>
</windows:ThemedWindow>
