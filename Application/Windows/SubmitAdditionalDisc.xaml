<windows:ThemedWindow x:Class="DiscRipper.Windows.SubmitAdditionalDisc"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:DiscRipper"
        xmlns:windows="clr-namespace:DiscRipper.Windows.Base"
        xmlns:controls="clr-namespace:DiscRipper.Controls"
        mc:Ignorable="d"
        Title="Add new disc to existing release" Height="450" Width="800">
    <Grid IsSharedSizeScope="True">
        <DockPanel>

            <ui:TitleBar DockPanel.Dock="Top" Title="{Binding Title, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}" FontWeight="SemiBold" />

            <controls:SubmitButtons DockPanel.Dock="Bottom" Submit="Submit_Click" Cancel="Cancel_Click" />

            <controls:DiscProperties DockPanel.Dock="Left" />
            <controls:DiscTitles x:Name="DiscTitles" />
        </DockPanel>
    </Grid>
</windows:ThemedWindow>
