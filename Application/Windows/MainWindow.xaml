<ui:FluentWindow x:Class="DiscRipper.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:DiscRipper"
        mc:Ignorable="d"
        Height="700" Width="1250"
        ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
        ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Auto"
        WindowCornerPreference="Round"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        
        <!-- Titlebar and combined menubar -->
        <DockPanel DockPanel.Dock="Top">
            <Control Width="48" Height="32" Margin="5" DockPanel.Dock="Left" Template="{StaticResource GitHubIconTemplate}" />
            <Menu DockPanel.Dock="Left">
                <MenuItem Header="_File">
                    <MenuItem Header="_Settings" Click="Settings_Click"/>
                    <Separator/>
                    <MenuItem Header="Rescan Drives" Click="RescanDrives_Click"/>
                    <Separator/>
                    <MenuItem Header="_Exit" Click="Exit_Click"/>
                </MenuItem>
                <MenuItem Header="_Session">
                    <MenuItem Header="_Load previous session" Click="LoadPreviousSession_Click"/>
                    <Separator/>
                    <MenuItem Header="Add new disc to existing release" Click="AddNewDisc_Click"/>
                </MenuItem>
                <MenuItem Header="_Debug" x:Name="DebugMenu">
                    <MenuItem Header="Test new release" Click="TestRelease_Click"/>
                    <MenuItem Header="Test new disc" Click="TestDisc_Click"/>
                </MenuItem>
            </Menu>
            <Grid>
                <Border IsHitTestVisible="False" CornerRadius="8" Padding="5" Background="{ui:ThemeResource AccentTextFillColorTertiaryBrush}" HorizontalAlignment="Left" VerticalAlignment="Center">
                    <TextBlock Text="Disc Ripper" IsHitTestVisible="False" FontWeight="SemiBold" Foreground="{ui:ThemeResource TextFillColorInverseBrush}" />
                </Border>
                <ui:TitleBar>
                    <ui:TitleBar.TrailingContent>
                        <WrapPanel Margin="0,0,5,0">
                            <ui:Button Appearance="Caution">Beta</ui:Button>
                            <ui:Button Width="48" Height="32" Margin="5" Click="GotoGithub_Click">
                                <Control Template="{StaticResource GitHubIconTemplate}" />
                            </ui:Button>
                            <ui:Button Width="48" Height="32" Click="SwitchTheme_Click">
                                <Viewbox Stretch="Uniform">
                                    <ContentControl Content="{StaticResource ThemeSwitchIcon}" />
                                </Viewbox>
                            </ui:Button>
                        </WrapPanel>
                    </ui:TitleBar.TrailingContent>
                </ui:TitleBar>
            </Grid>
        </DockPanel>
        
        <!-- Drives listing -->
        <DockPanel Margin="5">
            <Label DockPanel.Dock="Top" Target="{Binding ElementName=Drives}" Content="Drives"  Margin="10,0,0,5" />
            <ui:DataGrid x:Name="Drives" DockPanel.Dock="Left" ItemsSource="{Binding Drives}" AutoGenerateColumns="False" Width="650" IsReadOnly="True" MouseDoubleClick="Drives_MouseDoubleClick" >
                <ui:DataGrid.Columns>
                    <DataGridTextColumn Header="Type" Binding="{Binding DiscType}" />
                    <DataGridTextColumn Header="Path" Binding="{Binding DrivePath}" />
                    <DataGridTextColumn Header="Name" Binding="{Binding DriveName}" />
                    <DataGridTextColumn Header="Detected Media" Binding="{Binding MediaTitle}" />
                </ui:DataGrid.Columns>
            </ui:DataGrid>
        </DockPanel>

        <!-- Primary interaction choices -->
        <StackPanel Margin="5" Width="200">
            <Label Content="What do you want to do today?" Margin="5,0,0,5" />
            <ui:Button Appearance="Primary" Margin="5" Click="Rip_Click" HorizontalAlignment="Stretch">
                <TextBlock Text="Rip" FontSize="48" Margin="10" />
            </ui:Button>
            <Button Margin="5" Click="SubmitNewRelease_Click" HorizontalAlignment="Stretch">
                <TextBlock Text="Submit new release" FontSize="38" Margin="10" TextWrapping="Wrap" />
            </Button>
            <Button Margin="5" Click="AddNewDisc_Click" HorizontalAlignment="Stretch">
                <TextBlock Text="Add disc to release" FontSize="38" Margin="10" TextWrapping="Wrap" />
            </Button>
        </StackPanel>
        
        <!-- Log -->
        <DockPanel Margin="5">
            <Label DockPanel.Dock="Top" Target="{Binding ElementName=Feedback}" Content="Log" Margin="5,0,0,5" />
            <local:Feedback x:Name="Feedback" />
        </DockPanel>
    </DockPanel>
</ui:FluentWindow>
