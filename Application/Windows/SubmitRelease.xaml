<windows:ThemedWindow x:Class="DiscRipper.SubmitRelease"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:DiscRipper"
        xmlns:windows="clr-namespace:DiscRipper.Windows.Base"
        xmlns:localtddb="clr-namespace:DiscRipper.TheDiscDb;assembly=DiscRipper.TheDiscDb"
        xmlns:types="clr-namespace:DiscRipper.Types;assembly=DiscRipper.Types"
        xmlns:controls="clr-namespace:DiscRipper.Controls"
        mc:Ignorable="d"
        Title="Submit new release" Height="750" Width="1024">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <ui:TitleBar Grid.Row="0" Title="{Binding Title, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}" FontWeight="SemiBold" />
        
        <Grid Grid.Row="1" IsSharedSizeScope="True">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                    <StackPanel>
                        <controls:ReleaseProperties />
                        <controls:DiscProperties />
                    </StackPanel>
                </ScrollViewer>

                <controls:DiscTitles Grid.Column="1" VerticalAlignment="Stretch" />

            </Grid>

            <controls:SubmitButtons Grid.Row="1" Submit="Submit_Click" Cancel="Cancel_Click" SwitchToGuidedView="GuidedViewSwitch_Click" />
        </Grid>
    </Grid>
</windows:ThemedWindow>
