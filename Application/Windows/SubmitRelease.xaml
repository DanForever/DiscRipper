<windows:ThemedWindow x:Class="DiscRipper.SubmitRelease"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:DiscRipper"
        xmlns:windows="clr-namespace:DiscRipper.Windows.Base"
        xmlns:localtddb="clr-namespace:DiscRipper.TheDiscDb;assembly=DiscRipper.TheDiscDb"
        xmlns:types="clr-namespace:DiscRipper.Types;assembly=DiscRipper.Types"
        xmlns:controls="clr-namespace:DiscRipper.Controls"
        mc:Ignorable="d"
        Title="SubmitNewDisc" Height="600" Width="1024">
    <DockPanel>
        <ui:TitleBar Title="{Binding Title, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}" FontWeight="SemiBold" DockPanel.Dock="Top" />
        <Grid IsSharedSizeScope="True">
            <DockPanel>

                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
                    <ui:Button Margin="10" Click="Cancel_Click" VerticalAlignment="Stretch">
                        <TextBlock Text="Cancel" FontSize="32" Margin="20,10,20,10" />
                    </ui:Button>
                    <ui:Button Appearance="Primary" Margin="10" Click="Submit_Click" VerticalAlignment="Stretch">
                        <TextBlock Text="Submit!" FontSize="48" Margin="20,10,20,10" />
                    </ui:Button>
                </StackPanel>

                <StackPanel DockPanel.Dock="Left">
                    <controls:ReleaseProperties MediaTypeChanged="MediaType_Changed" />
                    <controls:DiscProperties />
                </StackPanel>
                <controls:DiscTitles x:Name="DiscTitles" />
            </DockPanel>
        </Grid>
    </DockPanel>
</windows:ThemedWindow>
