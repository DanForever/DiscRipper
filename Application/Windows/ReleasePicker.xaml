<windows:ThemedWindow x:Class="DiscRipper.Windows.ReleasePicker"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:DiscRipper.Windows"
        xmlns:windows="clr-namespace:DiscRipper.Windows.Base"
        xmlns:types="clr-namespace:DiscRipper.Types;assembly=DiscRipper.Types"
        mc:Ignorable="d"
        Title="Pick existing release to add a new disc to" Height="450" Width="800">
    <DockPanel Margin="5">
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <ui:TitleBar Grid.Column="1" FontWeight="SemiBold" />
            <ui:TextBox Grid.Column="0" FontSize="16" Text="{Binding FilterText, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Search" VerticalAlignment="Top" VerticalContentAlignment="Center" />
        </Grid>
        
        <TreeView ItemsSource="{Binding FilteredMedia}" FontSize="14" MouseDoubleClick="Tree_DoubleClick">
            <TreeView.Resources>
                <HierarchicalDataTemplate DataType="{x:Type types:Media}" ItemsSource="{Binding Releases}">
                    <TextBlock Text="{Binding Title}" />
                </HierarchicalDataTemplate>
                <HierarchicalDataTemplate DataType="{x:Type types:Release}">
                    <TextBlock Text="{Binding Slug}" />
                    <!-- Button MouseDoubleClick="ReleaseSlug_MouseDoubleClick" Margin="2" Padding="3">
                        <TextBlock Text="{Binding Slug}" />
                    </Button -->
                </HierarchicalDataTemplate>
            </TreeView.Resources>
        </TreeView>
    </DockPanel>
</windows:ThemedWindow>
