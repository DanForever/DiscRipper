<UserControl x:Class="DiscRipper.Controls.Guided.RegionCode"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:localtddb="clr-namespace:DiscRipper.TheDiscDb;assembly=DiscRipper.TheDiscDb"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DiscRipper.Controls.Guided"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ObjectDataProvider x:Key="RegionCodes"
                            MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="localtddb:RegionCode"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <ui:Anchor Margin="0,0,0,5" NavigateUri="https://en.wikipedia.org/wiki/DVD_region_code#Region_codes_and_countries" Content="DVD region codes at Wikipedia" Icon="{ui:SymbolIcon Link24}" />
            <ui:Anchor Margin="0,0,0,5" NavigateUri="https://en.wikipedia.org/wiki/DVD_region_code#Blu-ray_disc_region_codes" Content="Blu-ray region codes at Wikipedia" Icon="{ui:SymbolIcon Link24}" />
        </StackPanel>
        <ComboBox Grid.Row="1" ItemsSource="{Binding Source={StaticResource RegionCodes}}" SelectedItem="{Binding RegionCode, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</UserControl>
