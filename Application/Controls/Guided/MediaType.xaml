<UserControl x:Class="DiscRipper.Controls.Guided.MediaType"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiscRipper.Controls.Guided"
             xmlns:util="clr-namespace:DiscRipper.Utilities"
             xmlns:types="clr-namespace:DiscRipper.Types;assembly=DiscRipper.Types"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ObjectDataProvider MethodName="GetValues" ObjectType="{x:Type sys:Enum}" x:Key="MyEnumValues">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="types:MediaTypes"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>
    <Grid>
        <ComboBox ItemsSource="{Binding Mode=OneTime, Source={StaticResource MyEnumValues}}" SelectedValue="{Binding MediaType_New}" util:FocusHelper.AutoFocus="True" />
    </Grid>
</UserControl>
