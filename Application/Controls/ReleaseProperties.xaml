<UserControl x:Class="DiscRipper.Controls.ReleaseProperties"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:localtddb="clr-namespace:DiscRipper.TheDiscDb;assembly=DiscRipper.TheDiscDb"
             xmlns:local="clr-namespace:DiscRipper"
             xmlns:converters="clr-namespace:DiscRipper.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <converters:StringToCollapsedConverter x:Key="CollapseOnMovie" MatchString="Movie" />
    </UserControl.Resources>
    <Grid IsSharedSizeScope="True">

        <StackPanel DockPanel.Dock="Left" MinWidth="400">
            <StackPanel.Resources>
                <Style TargetType="TextBox">
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>

                <ObjectDataProvider x:Key="RegionCodes"
                            MethodName="GetValues"
                            ObjectType="{x:Type sys:Enum}">
                    <ObjectDataProvider.MethodParameters>
                        <x:Type TypeName="localtddb:RegionCode"/>
                    </ObjectDataProvider.MethodParameters>
                </ObjectDataProvider>
            </StackPanel.Resources>

            <TextBlock Text="Release" />

            <local:Setting Label="TMDB ID">
                <ui:TextBox Text="{Binding TMDB, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="1234 or https://www.themoviedb.org/movie/1234-a-movie-url" />
            </local:Setting>
            <local:Setting Label="Media type">
                <ComboBox ItemsSource="{Binding Mode=OneTime, Source={x:Static local:SubmitRelease.MediaTypes }}" SelectedValue="{Binding MediaType}" SelectionChanged="OnMediaTypeChanged" />
            </local:Setting>
            <local:Setting Label="Season" Visibility="{Binding MediaType, Mode=OneWay, Converter={StaticResource CollapseOnMovie}}">
                <ui:TextBox Text="{Binding Season, UpdateSourceTrigger=PropertyChanged, TargetNullValue=''}" />
            </local:Setting>
            <local:Setting Label="Release slug">
                <ui:TextBox Text="{Binding ReleaseSlug, UpdateSourceTrigger=PropertyChanged}" />
            </local:Setting>
            <local:Setting Label="UPC">
                <ui:TextBox Text="{Binding UPC, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Barcode number" />
            </local:Setting>
            <local:Setting Label="ASIN">
                <ui:TextBox Text="{Binding ASIN, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="Amazon identification" />
            </local:Setting>
            <local:Setting Label="Release publication date">
                <DatePicker SelectedDate="{Binding PublicationDate, UpdateSourceTrigger=PropertyChanged}" />
            </local:Setting>
            <local:Setting Label="Front cover image URL">
                <ui:TextBox Text="{Binding FrontCoverUrl, UpdateSourceTrigger=PropertyChanged}" />
            </local:Setting>
            <local:Setting Label="Back cover image URL">
                <ui:TextBox Text="{Binding BackCoverUrl, UpdateSourceTrigger=PropertyChanged}" />
            </local:Setting>
            <local:Setting Label="Name of release/edition">
                <ui:TextBox Text="{Binding EditionName, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="The name of this particular release" />
            </local:Setting>
            <local:Setting Label="Region code">
                <ComboBox ItemsSource="{Binding Source={StaticResource RegionCodes}}" SelectedItem="{Binding RegionCode, UpdateSourceTrigger=PropertyChanged}" />
            </local:Setting>
            <local:Setting Label="Locale">
                <ComboBox ItemsSource="{Binding Mode=OneWay, Source={x:Static local:SubmitRelease.AllRegions }}" SelectedValuePath="Name" DisplayMemberPath="DisplayName" SelectedValue="{Binding Locale, UpdateSourceTrigger=PropertyChanged}" IsTextSearchEnabled="True" IsTextSearchCaseSensitive="False" IsEditable="True" />
            </local:Setting>
        </StackPanel>

    </Grid>
</UserControl>
