<UserControl x:Class="DiscRipper.Controls.DiscProperties"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DiscRipper"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid IsSharedSizeScope="True">
        <StackPanel MinWidth="400">

            <StackPanel.Resources>
                <Style TargetType="TextBox">
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                </Style>
            </StackPanel.Resources>

            <TextBlock Text="Disc" />

            <local:Setting Label="Disc format">
                <ComboBox ItemsSource="{Binding Mode=OneTime, Source={x:Static local:SubmitRelease.DiscFormats }}" SelectedValue="{Binding DiscFormat}" />
            </local:Setting>
            <local:Setting Label="Name of disc">
                <TextBox Text="{Binding DiscTitle, UpdateSourceTrigger=PropertyChanged}" />
            </local:Setting>
            <local:Setting Label="Disc slug">
                <TextBox Text="{Binding DiscSlug, UpdateSourceTrigger=PropertyChanged}" />
            </local:Setting>
        </StackPanel>
        
    </Grid>
</UserControl>
